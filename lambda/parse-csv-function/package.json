{
  "name": "parse-csv-function",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "package": "npm run build && zip -r lambda-package.zip dist node_modules"
  },
  "dependencies": {
    "aws-sdk": "^2.1044.0",
    "csv-parser": "^3.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.114",
    "@types/node": "^16.11.15",
    "typescript": "^4.5.4"
  }
}
